apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor-service
  namespace: bookmarket
  labels:
    app: vendor-service
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: vendor-service
      version: v1
  template:
    metadata:
      labels:
        app: vendor-service
        version: v1
    spec:
      containers:
      - name: vendor-service
        image: bookmarket/vendor-service:latest
        ports:
        - containerPort: 3002
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: url
        - name: REDIS_URL
          value: "redis://redis-service:6379"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: jwt-secret
              key: secret
        - name: PORT
          value: "3002"
        - name: ENVIRONMENT
          value: "production"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: vendor-service
  namespace: bookmarket
  labels:
    app: vendor-service
spec:
  selector:
    app: vendor-service
  ports:
  - port: 80
    targetPort: 3002
    protocol: TCP
  type: ClusterIP
